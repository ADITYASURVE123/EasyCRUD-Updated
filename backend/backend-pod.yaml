apiVersion: v1
kind: Pod
metadata:
  name: backend
  labels:
    app: backend
spec:
  containers:
    - name: backend
      image: adityasurve/easycrud2-jenkins:backend
      ports:
        - containerPort: 80
